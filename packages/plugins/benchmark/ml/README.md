# ML Detection — Commercial

The ML injection detection model training code and weights are in the commercial `governance-ml` package.

The open-source benchmark provides:
- The evaluation dataset and runner
- The `InjectionClassifier` interface for plugging in any ML model
- The regex-based detector (zero dependencies)

For ML-powered detection, see `governance-sdk-ml` (commercial tier).
